// this is yet a mock since I'm building the UI first
export type Product = {
    id: string;
    name: string;
    imageUrl: string;
    description: string;
    category: string;
    price: number;
    salesPrice?: number;
};
